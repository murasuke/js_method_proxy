/**
 * 加算
 * @param {number} val1
 * @param {number} val2
 * @returns
 */
export function add(val1, val2) {
  return val1 + val2;
}

/**
 * 文字結合
 * @param {string} s1
 * @param {string} s2
 * @returns
 */
export const strcat = (s1, s2) => s1 + s2;

export let prop = 'prop value';
